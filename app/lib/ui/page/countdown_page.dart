import 'package:flutter/material.dart';

class CountdownPagee extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('定时任务'),
        brightness: Brightness.dark,
      ),
      body: Center(
          child: Text('text')
      ),
    );
  }
}
